model:
  class_name: model.spa3r.Spa3R
  embed_dim: 768
  num_heads: 12
  aggregator_depth: 12
  decoder_depth: 12
  patch_size: 14
  image_size: 256
  num_register_tokens: 4
  mlp_ratio: 4.0
  init_values: 0.01
  qk_norm: true
  rope_freq: 100
  patch_embed: dinov2_vitb14_reg
  backbone_pretrained: null
  freeze_backbone: true
  pose_head_depth: 4
  image_tokenizer:
    image_size: 256
    patch_size: 14
    in_channels: 3
  target_image:
    height: 256
    width: 256
    patch_size: 14
    in_channels: 6

training:
  amp_dtype: bf16
  api_key_path: ./configs/api_keys.yaml
  batch_size_per_gpu: 1
  beta1: 0.9
  beta2: 0.95
  allowed_gradnorm_factor: 2.0
  center_crop: true
  scene_scale_factor: 1.35
  checkpoint_dir: ./experiments/checkpoints/spa3r_dl3dv_eval
  checkpoint_every: 2000
  dataset_name: data.dataset_scene.Dataset
  dataset_path: data/dl3dv_benchmark.txt
  grad_accum_steps: 1
  grad_checkpoint_every: 1
  grad_clip_norm: 1.0

  l2_loss_weight: 1.0
  lpips_loss_weight: 0.0
  perceptual_loss_weight: 0.0
  feature_loss_weight: 1.0
  pose_align_loss_weight: 1.0

  lr: 0.0004
  train_steps: 100000
  no_pass_steps: 100

  num_views: 2
  num_input_views: 1
  num_target_views: 1
  target_has_input: false
  random_split: false
  view_selector:
    type: two_frame
    min_frame_dist: 16
    max_frame_dist: 48
    use_curriculum: false
  num_threads: 8
  num_workers: 4
  prefetch_factor: 8
  print_every: 20

  square_crop: true
  use_amp: true
  use_rel_pose: false
  use_tf32: true
  vis_every: 1000

  wandb_project: spa3r
  wandb_exp_name: spa3r_dl3dv_eval
  wandb_log_every: 50

  warmup: 3000
  weight_decay: 0.05

  render_video: render_video

inference:
  if_inference: True
  compute_metrics: True
  view_idx_file_path: ./data/rayzer_evaluation_index_dl3dv_even.json
  render_video: False
  render_video_config:
    traj_type: interpolate
    num_frames: 30
    loop_video: True
    order_poses: False
